export type ApiSquadResponse = {
  id: string
  name: string
  created_at: string
  members_count: number
}

export type Squad = {
  id: string
  name: string
  createdAt: string
  membersCount: number
}
